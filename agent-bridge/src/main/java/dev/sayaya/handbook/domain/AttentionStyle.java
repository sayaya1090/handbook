package dev.sayaya.handbook.domain;

/**
 * attention 커맨드의 시각적 표현 스타일.
 * 백엔드 agent-protocol 의 동일 enum 에 대응하는 GWT 호환 버전.
 */
public enum AttentionStyle {
    COACHMARK,
    SPOTLIGHT,
    PULSE,
    ARROW,
    BADGE
}
