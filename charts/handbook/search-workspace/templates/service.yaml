apiVersion: v1
kind: Service
metadata:
  name: service-search-workspace
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: search-workspace
  ports:
    - name: http-search-workspace
      protocol: TCP
      port: 8080
      targetPort: 8080
      appProtocol: http
