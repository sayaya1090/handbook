apiVersion: v1
kind: Service
metadata:
  name: service-document-query
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: document-query
  ports:
    - name: http-document-query
      protocol: TCP
      port: 8080
      targetPort: 8080
      appProtocol: http
