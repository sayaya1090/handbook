apiVersion: v1
kind: Service
metadata:
  name: service-search-type
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: search-type
  ports:
    - name: http-search-type
      protocol: TCP
      port: 8080
      targetPort: 8080
      appProtocol: http
