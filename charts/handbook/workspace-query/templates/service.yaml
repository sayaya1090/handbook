apiVersion: v1
kind: Service
metadata:
  name: service-workspace-query
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: workspace-query
  ports:
    - name: http-workspace-query
      protocol: TCP
      port: 8080
      targetPort: 8080
      appProtocol: http
