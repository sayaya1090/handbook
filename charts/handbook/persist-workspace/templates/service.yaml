apiVersion: v1
kind: Service
metadata:
  name: service-persist-workspace
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: persist-workspace
  ports:
    - name: http-persist-workspace
      protocol: TCP
      port: 8080
      targetPort: 8080
      appProtocol: http
