apiVersion: v1
kind: Service
metadata:
  name: event-broadcaster
spec:
  selector:
    app: event-broadcaster
  ports:
    - name: http-event
      protocol: TCP
      port: 8080
      targetPort: 8080
      appProtocol: http
