apiVersion: v1
kind: Service
metadata:
  name: service-type-command
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: type-command
  ports:
    - name: http-type-command
      protocol: TCP
      port: 8080
      targetPort: 8080
      appProtocol: http
