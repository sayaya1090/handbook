annotations:
  charts.openshift.io/name: type-command
apiVersion: v2
kubeVersion: '>= 1.20.0-0'
name: type-command
version: 0.1.0
description: Persist type (write side) service for handbook — types/attributes/layouts CUD + Kafka publisher
dependencies:
  - name: handbook-lib
    version: 0.1.0
    repository: file://../../handbook-lib
