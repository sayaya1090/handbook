annotations:
  charts.openshift.io/name: type-query
apiVersion: v2
kubeVersion: '>= 1.20.0-0'
name: type-query
version: 0.1.0
description: Search type (read side + menu supplier) service for handbook — types/attributes/layouts 조회
dependencies:
  - name: handbook-lib
    version: 0.1.0
    repository: file://../../handbook-lib
