name: 문서 검색 모듈 배포
on:
  push:
    paths:
      - search-document/**
      - activity/**
  workflow_dispatch:
jobs:
  deploy:
    uses: ./.github/workflows/_jvm-deploy.yaml
    with:
      submodule: search-document
    secrets: inherit
