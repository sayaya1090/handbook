name: 게이트웨이 모듈 배포
on:
  push:
    paths:
      - gateway/**
  workflow_dispatch:
jobs:
  deploy:
    uses: ./.github/workflows/_jvm-deploy.yaml
    with:
      submodule: gateway
    secrets: inherit
