name: App 모듈 빌드
on:
  push:
    paths:
      - app/**
  workflow_dispatch:
permissions:
  contents: write
jobs:
  build:
    uses: ./.github/workflows/_frontend-deploy.yaml
    with:
      submodule: app
    secrets: inherit
