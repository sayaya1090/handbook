name: 이벤트 송출 모듈 배포
on:
  push:
    paths:
      - event-broadcaster/**
  workflow_dispatch:
jobs:
  deploy:
    uses: ./.github/workflows/_jvm-deploy.yaml
    with:
      submodule: event-broadcaster
    secrets: inherit
